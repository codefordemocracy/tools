<h5 class="text-2xl text-dark-gray mb-2">Choose Lists</h5>
<p class="font-light text-sm leading-relaxed">Continue by configuring lists to use as inputs for the recipe.</p>
<template v-if="_.includes(store.state.workflow.valid, store.state.workflow.tab-1)">
  <p class="text-xs mt-3 mb-3">Click on the buttons with placeholders for each slot in the recipe in order to select a list for that slot in the recipe.</p>
  <p class="text-xs">You'll be able to continue on to the next step once you've chosen a list for each slot in the recipe.</p>
</template>
